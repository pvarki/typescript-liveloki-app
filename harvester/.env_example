# Bluesky harvester configuration
# Comma-separated list of case-insensitive keywords
HARVESTER_KEYWORDS="trump"

# Jetstream endpoint URL (defaults to public us-east if omitted)
#JETSTREAM_URL=wss://jetstream2.us-east.bsky.network/subscribe

# Comma-separated list of collections to request (defaults to app.bsky.feed.post)
#JETSTREAM_WANTED_COLLECTIONS=app.bsky.feed.post

# Set to 0 to emit nothing when no keywords are configured (default is 1 = pass through all)
#HARVESTER_MATCH_ALL_IF_NO_KEYWORDS=1

# Reconnect behavior (1 = enabled, 0 = disabled)
#HARVESTER_RECONNECT=1
#HARVESTER_RECONNECT_DELAY_MS=1000
#HARVESTER_RECONNECT_DELAY_MAX_MS=30000

# Battlelog integration
# Set to 1 to enable sending matched events to Battlelog
HARVESTER_SEND_TO_BATTLELOG=1

# Base URL for the Battlelog API (inside Docker this is typically http://app:3000)
# When running Battlelog via docker-compose in the parent project and harvester in a separate project,
# use host.docker.internal to reach the host, which forwards to the Battlelog container port.
BATTLELOG_BASE_URL=http://host.docker.internal:3000

# Optional override for the events path (default /api/events)
#BATTLELOG_EVENTS_PATH=/api/events

# Shared secret used to identify the harvester to the Battlelog backend
HARVESTER_API_KEY=testtesttest
