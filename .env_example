POSTGRES_PASSWORD=change_me
POSTGRES_USER=livelogiuser
POSTGRES_HOST=postgres
POSTGRES_DB=livelogi
BASE_URL=/
API_BASE_URL=http://app:3000

# Set true only when testing proxied/RM-auth behavior locally.
RM_MTLS_USER_ENFORCE=false

# Set false to hide Battlelog from Deploy App main UI cards.
#BL_MAIN_UI_CARD_VISIBLE=false

# Set true to expose Swagger UI at /api-docs and OpenAPI JSON at /openapi.json.
USE_SWAGGER=false

# Set to 1 or higher when running behind reverse proxies.
#BL_TRUST_PROXY_HOPS=1
